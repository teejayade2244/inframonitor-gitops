# Staging is the formal name for what has, until now, been the only (undifferentiated)
# deployment of this chart. Every value here is byte-for-byte what values.yaml used to contain -
# nothing about the live inframonitor namespace deployment changes when this file is used.
namespace: inframonitor
replicaCount: 1
serviceAccountName: events-service-sa
image:
  tag: 7a920b5
env:
  COSMOS_ENDPOINT: "https://inframonitor-aks-cosmos-eastus2.documents.azure.com:443/"
  COSMOS_DATABASE: "InfraMonitorDB"
  SERVICEBUS_NAMESPACE: "inframonitor-aks-svcbus.servicebus.windows.net"
  SERVICEBUS_TOPIC: "infrastructure-events"
  AZURE_CLIENT_ID: "66e64342-7894-4bf0-8047-8598259734e2"
